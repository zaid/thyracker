.sample
  .taken_on
    %h4 Taken on:
    = @sample.taken_on
  .tsh
    %h4 TSH:
    = @sample.tsh
  .t3
    %h4 T3:
    = @sample.t3
  .t4
    %h4 T4:
    = @sample.t4
  .sample-actions
    = link_to "Update", edit_sample_path(@sample), class: "sample-update"
    = link_to "Delete", sample_path(@sample), method: :delete, class: "sample-delete"

